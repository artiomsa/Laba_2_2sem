#pragma once
template <class T>
T Summ_items(T& item1, T& item2)
{
    T itemRes = item1 + item2;
    return itemRes;
}
template <class T>
T Comp_items(T& item1, T& item2)
{
    T itemRes = item1 * item2;
    return itemRes;
}